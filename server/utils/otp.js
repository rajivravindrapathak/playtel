// Function to send OTP (for demonstration purposes)
exports.sendOTP = function (mobileNumber, otp) {
    // Replace this with actual code to send OTP via SMS or another channel
    console.log(`Sending OTP to ${mobileNumber}: ${otp}`);
};